{
  "name": "ativelo-platform",
  "version": "0.1.0",
  "private": true,
  "description": "Plataforma SaaS multiempresa para academias, profissionais e alunos.",
  "packageManager": "npm@11.16.0",
  "engines": {
    "node": ">=22.13.0"
  },
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "dev": "npm run dev --workspace=@ativelo/web",
    "build": "npm run build --workspace=@ativelo/core && npm run build --workspace=@ativelo/web",
    "lint": "npm run lint --workspaces --if-present",
    "typecheck": "npm run typecheck --workspaces --if-present",
    "test": "npm run test --workspaces --if-present",
    "test:coverage": "npm run test:coverage --workspace=@ativelo/web",
    "test:e2e": "npm run test:e2e --workspace=@ativelo/web",
    "check": "npm run lint && npm run typecheck && npm run test && npm run build",
    "clean": "node scripts/clean.mjs",
    "reinstall": "npm run clean && npm install"
  }
}
